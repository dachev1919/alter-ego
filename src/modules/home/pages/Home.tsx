import React, { FC } from 'react';
import { Container } from '@mui/material';

const Home: FC = () => {
	return (
		<section>
			<Container maxWidth='xl'>Home</Container>
		</section>
	);
};

export default Home;
