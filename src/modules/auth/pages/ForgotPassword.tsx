import { FC } from 'react';
import Section from '../../../common/components/section/Section';

const ForgotPassword: FC = () => {
	return <Section title='Password Recovery'></Section>;
};

export default ForgotPassword;
